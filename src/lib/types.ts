export type Song = {
  _id: string;
  title: string;
  description: string;
  imageUrl: string;
  songUrl: string;
};

export type User = {
  _id: string;
  email: string;
  name: string;
  dob: string;
  phone: string;
  avatarUrl: string;
};
